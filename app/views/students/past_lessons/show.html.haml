%h2 #{@student.name}さん受講済レッスン一覧
- @lessons.each do |lesson|
  %p
    = l lesson.started_at
    = link_to '詳細', lesson_path(lesson)
    = link_to 'レビューを確認する', review_path(lesson.review) if lesson.review.present?

= paginate @lessons
