%h2 作成済レッスン一覧
- @lessons.each do |lesson|
  %p
    = l lesson.started_at
    = link_to '詳細', lesson_path(lesson)
    - if lesson.lesson_reservation.present?
      = link_to "#{lesson.lesson_reservation.student.name}さんのレッスン履歴", students_past_lesson_path(student: lesson.lesson_reservation.student)
      ※予約あり
    = link_to '削除', lesson_path(lesson), method: :delete
%p= link_to 'レッスンを作成する', new_lesson_path
%p= link_to 'レッスン履歴', teachers_past_lesson_path

= paginate @lessons
